import React from "react";
import Header from "../../components/Header/Header";

const DataView = () => {
  return (
    <div>
      <Header title={`UK Crime in ${"enter postcode from search"}`} />
    </div>
  );
};

export default DataView;
